import 'package:flutter/material.dart';

class AppColors {
  // Bleu principal proche de ta maquette
  static const primary = Color(0xFF1976FF);

  // Light
  static const lightBg = Color(0xFFF6F7FB);
  static const lightSurface = Colors.white;
  static const lightText = Color(0xFF0B1220);
  static const lightSubText = Color(0xFF6B7280);
  static const lightBorder = Color(0xFFE5E7EB);

  // Dark (proche du dashboard)
  static const darkBg = Color(0xFF0B1220);
  static const darkSurface = Color(0xFF121A2A);
  static const darkText = Color(0xFFF3F4F6);
  static const darkSubText = Color(0xFF9CA3AF);
  static const darkBorder = Color(0xFF253046);
}